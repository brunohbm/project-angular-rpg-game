import { SpellPopoverDirective } from './spell-popover.directive';

describe('SpellPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SpellPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
