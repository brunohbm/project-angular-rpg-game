import { ItemPopoverDirective } from "./item-popover.directive";

describe('ItemPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
